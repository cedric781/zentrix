# scripts/

This directory holds **at most 5 active operational scripts**, per `docs/LESSONS_FROM_WAGER.md` R3.

If you find yourself writing a 6th script, the answer is not "add it" — the answer is "promote one to a metric or alert in the production app, then archive it".

Archived scripts go in `scripts/archive/` (read-only history).

## Currently active

(none yet)